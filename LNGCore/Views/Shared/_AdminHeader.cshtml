<nav class="navbar navbar-expand-lg navbar-dark bg-dark">
    <div class="container">
        <a class="navbar-brand" href="@Url.Action("Index","Admin")">LNG Laserworks</a>
        <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="navbarNav">
            <ul class="navbar-nav">
                @{
                    var navItems = new Dictionary<string, string> { { "Index", "Home" }, { "Invoices", "Invoices" }, { "Customers", "Customers" }, { "Configuration", "Configuration" } }; //action, desc

                    foreach (var nav in navItems)
                    {
                        <li class="nav-item @(nav.Key == ViewBag.ActiveAction ? "active" : "")">
                            <a class="nav-link" asp-controller="Admin" asp-action="@nav.Key">@nav.Value</a>
                        </li>
                    }
                }
            </ul>
        </div>
    </div>
</nav>